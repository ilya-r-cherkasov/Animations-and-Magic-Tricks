//
//  ColorChanger.swift
//  Animations-and-Magic-Tricks
//
//  Created by Ilya Cherkasov on 12.09.2022.
//

import UIKit

final class ColorChanger {

//    func erasureColor(from image: UIImage) -> UIImage {
//        let rect = CGRect(origin: .zero, size: image.size)
//        let renderer = UIGraphicsImageRenderer(size: image.size)
//        let newImage = renderer.image { context in
//            let context = context.cgContext
//            let bitMapData = image.cgImage?.dataProvider?.data
//            context.draw(image.cgImage!, in: rect)
//            let pointer = context.data
//            let capacity = Int(image.size.width * image.size.height)
//            let pixelBuffer = pointer?.bindMemory(to: UInt32.self, capacity: capacity)
//            for pixel in 0...capacity - 1 {
//                bitMapData?[pixel] = 0x0000FF
//            }
//        }
//        return newImage
//    }


}
